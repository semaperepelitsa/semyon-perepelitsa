%article
  - if post.title.present?
    %h1= post.title
  != post.body
  %details
    = timestamps_for post
    = link_to '*', [:edit, post] if admin?
    = link_to '#', post, :title => t('.permalink')
